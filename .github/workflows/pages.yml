name: Website

on:
  push:
    branches: [master]
    paths:
      - site/**
      - .github/workflows/pages.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      # Each run package is authored as a directory so it can be tested and
      # edited directly. Publish the corresponding ZIP beside each package.
      - name: Package run tools
        run: |
          find site/r -type d -name 'run.zip.d' -print0 |
            while IFS= read -r -d '' package; do
              echo "Packaging $package"
              (
                cd "$package"
                zip -X -r ../run.zip . \
                  -x context.d.ts '*/context.d.ts' timestamp.tsr
              )
            done
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
